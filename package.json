{
  "name": "zabbix_rest_api",
  "version": "1.0.0",
  "description": "npm install express npm install body-parser --save npm install cookie-parser --save npm install multer --save     npm install zabbix-rpc  node main.js",
  "main": "main.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "zabbix-rpc": "^1.2.1"
  },
  "devDependencies": {
    "jsdoc": "^3.6.6"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
